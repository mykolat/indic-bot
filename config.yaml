trading:
  pairs: [BTCUSDT, ETHUSDT, SOLUSDT, BNBUSDT, XRPUSDT, DOGEUSDT, ADAUSDT, AVAXUSDT]
  maxLeverage: 25
  maxPositionPct: 60
  maxExposurePct: 150
  maxStopLossPct: 5
  maxLossUsd: 30
  maxLossPct: 10
  targetReturnPct: 100
  minTakeProfitPct: 5
  scalpingMinTakeProfitPct: 1.0
  scalpingMaxStopLossPct: 0.8
  minConfidence: 55
  churnCooldownMs: 900000
  loopIntervalMs: 60000
  newsRefreshIntervalH: 0.33
  newsMaxItems: 100
  stalePositionHours: 8
  maxHoldHours: 24
  fearGreedLeverageCap: 10
  weekendLeverageMultiplier: 0.5

positionManagement:
  enabled: true
  tp1CloseRatio: 0.5
  breakevenBufferPct: 0.1
  trailing:
    enabled: true
    callbackRatePct: 1.0
  regimeOverrides:
    Capitulation:
      tp1CloseRatio: 1.0
    Range:
      tp1CloseRatio: 0.7
      callbackRatePct: 0.5
    BullTrend:
      callbackRatePct: 1.5

openai:
  model: gpt-5.4
  fallbackModel: gpt-4o-mini

binance:
  testnet: false

webhook:
  port: 3000
